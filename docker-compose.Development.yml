version: '3.7'
services:
  ms-sql-server:
    image: mcr.microsoft.com/mssql/server:2017-latest
    container_name: sql_server_container_development
    restart: always
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: ${SQL_PASS}
      MSSQL_PID: Express

  webapi:
    restart: always
    container_name: webapi_container_Development
    image: webapi_image:Development
    build:
      context: .
      args:
        PASSWORD_CERT: ${PASSWORD_CERT}
        ENVIRONMENT: Development
        SQL_PASS: ${SQL_PASS}
    ports:
      - "5000:80"
      - "5001:443"
    links:
      - ms-sql-server
